import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    paddingTop: '20%',
  },
  button: {
    marginTop: 16,
  },
});

export default style;
